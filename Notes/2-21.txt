default mysql port 3306

nodemon refreshes server whenever a change is detected
    --watch server makes it so that it only checks the server folder

1 controller per table


below is a function:
    app.get("/", (req, res) => {
    
    })

//app gets (server, (request, response))

app.use() mounts packages
app.use('location', pack name)

require('/controller/user) returns what is exported 