synchonisly = all is received at one time, then runs the next function

asynchonist

http methods:
    get
    post = used for forms

    get: looks only at query parameters
        -used for receiving information
        -does not change the state of the program
        -has 1 parameter: query
    post: creates a new line and all parameters are posted after (not part of the URL)
        -post is for sending information
        -used when changing the state of the program
        -has 2 parameters: query and body
