modules: 
    helps limit scope of variables

scripts:
    order of scripts important

webpack
    runs through babble for translation 

Framework: Vue
    vue-cli - helps to sets up the initial package
        assumes webpack will be used and .vue files
        services: set of packages that run during development, not part of final application
        hot module reloading
        allows for code editing straight into testing rather than having to rebuild/compile/restart

    >> use vue ui

    >> vue create <name>

    !!install vetur!!