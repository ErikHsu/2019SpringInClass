SQL -- NODE -- BROWSER

surrogate key
# Natural keys almost always end up incorrect, not prefered when engineering database

** USERS **
id              #
created_at      # autocreated and in all tables
updated_at      #
password
firstName
lastName

(N / M)

** CONTACT METHODS **
id
created_at
updated_at
user_Id
type
value (unique)

(N / M)

** USER ROLES **

(M / N)

** ROLES **
value



# notes
- numbers (phone number, zip code) are strings unless calculations need to be done
- passwords should never be saved in plaintext, use one way hash
    - To check if password entered is the same as database, compare hash
